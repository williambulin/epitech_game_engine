#pragma once

#include <vector>

#include "AudioListener.hpp"

//Will be used in 3D audio later
class AudioListener {
public:
  explicit AudioListener()  = default;
  ~AudioListener()          = default;
};